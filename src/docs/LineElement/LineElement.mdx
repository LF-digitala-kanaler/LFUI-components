import { Meta, Canvas, Stories, Story, Subtitle, Controls } from '@storybook/blocks';
import * as LineElementStories from './lineElement.stories';
import { Badges } from '../../../.storybook/blocks/Badges';
import { Note } from '../../../.storybook/blocks/Note';

<Meta of={LineElementStories} />

# Line element <Badges of={LineElementStories} />

<Subtitle>
	The line element is used to separate parts of the page from each other. We use the html-tag hr and
	style it in the fashion of Länsförsäkringar.
</Subtitle>

<Canvas of={LineElementStories.Default} />

## Modifiers

### `.on-white-bg`

<Canvas of={LineElementStories.OnWhite} />
